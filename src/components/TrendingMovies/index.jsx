// import { getTrendingMovies } from '../../services/filmsApi';

const TrendingMovies = () => {
  return (
    <ul>
      <li>ghjkl;</li>
      {/* {getTrendingMovies().then(data =>
        data.map(({ id, original_name }) => <li key={id}>{original_name}</li>),
      )} */}
    </ul>
  );
};

export default TrendingMovies;
